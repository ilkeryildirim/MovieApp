import 'package:flutter/material.dart';

class AppColors {
  // Private constructor to prevent instantiation
  AppColors._();
  
  // Figma Design Colors
  static const Color primaryRed = Color(0xFFE50914);
  static const Color primaryColor = primaryRed;
  static const Color darkBackground = Color(0xFF000000);
  static const Color backgroundColor = Color(0xFF000000);
  static const Color inputBackground = Color(0xFF2D2D2D);
  static const Color cardColor = inputBackground;
  static const Color whiteText = Color(0xFFFFFFFF);
  static const Color grayText = Color(0xFF9CA3AF);
  static const Color inputBorder = Color(0xFF374151);
  static const Color errorColor = primaryRed;
  
  // Social button colors
  static const Color socialButtonBackground = Color(0xFF2D2D2D);
  
  // Gradient colors
  static const Color gradientStart = Color(0xFF1A1A1A);
  static const Color gradientEnd = Color(0xFF2D2D2D);
} 